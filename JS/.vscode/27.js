/**
 * @param {number[]} nums
 * @param {number} val
 * @return {number}
 */
var removeElement = function(nums, val) {
    var i = nums.length-1;
    while(i>0)
    {
        if(val == nums[i])
        num = nums.join(nums.slice(0,i-1),...nums.slice(i,nums.length-1))
    }

};